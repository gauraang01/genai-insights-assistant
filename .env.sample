# Postgres
POSTGRES_USER=genai
POSTGRES_PASSWORD=changeme
POSTGRES_DB=genai_db
POSTGRES_PORT=5432

# Chroma
CHROMA_HTTP_PORT=8000

# Streamlit
STREAMLIT_PORT=8501

# LLM config
# If you have an OpenAI key, put it in .env (do NOT commit)
OPENAI_API_KEY=

# Local Llama fallback (optional)
USE_LOCAL_LLAMA=false
LLAMA_MODEL_PATH= # path to local llama model if using fallback

# App runtime config
APP_ENV=development
